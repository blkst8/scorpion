apiVersion: v1
kind: Service
metadata:
  name: {{ include "scorpion.fullname" . }}
  labels:
    {{- include "scorpion.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "scorpion.selectorLabels" . | nindent 4 }}
  ports:
    - name: https
      port: {{ .Values.service.httpsPort }}
      targetPort: https
      protocol: TCP
    - name: metrics
      port: {{ .Values.service.metricsPort }}
      targetPort: metrics
      protocol: TCP

